server:
  port: 9003

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 配置Nacos地址作为服务注册中心

management:
  endpoints:
    web:
      exposure:
        include: "*"